<nav class="bg-slate-100 shadow-md w-full xl:w-[1120px] fixed top-0 left-1/2 -translate-x-1/2 z-50 mb-10 rounded-lg">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex justify-between h-16 items-center">
      <span class="flex flex-col">
        <span class="font-bold text-xl text-gray-800">Sergio Méndez</span>
        <span class="text-base font-medium text-gray-600 tracking-wide">T.S Desarrollo de Aplicaciones Multiplataforma</span>
      </span>
      <!-- Menú para pantallas grandes -->
      <div class="hidden md:flex space-x-6">
        <a href="#proyectos" class="text-gray-700 hover:text-blue-600">Proyectos</a>
        <a href="#trayectoria" class="text-gray-700 hover:text-blue-600">Trayectoria</a>
        <a href="#sobre-mi" class="text-gray-700 hover:text-blue-600">Sobre mí</a>
        <a href="#contacto" class="text-gray-700 hover:text-blue-600">Contacto</a>
      </div>
      <!-- Botón hamburguesa para móvil -->
      <button id="menu-btn" class="md:hidden flex items-center" aria-label="Abrir menú">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
            d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
    <!-- Menú móvil (oculto por defecto, solo visible en móvil) -->
    <div id="mobile-menu" class="hidden flex-col space-y-2 pb-4 md:hidden">
      <a href="#proyectos" class="block py-2 text-gray-700 hover:text-blue-600">Proyectos</a>
      <a href="#trayectoria" class="block py-2 text-gray-700 hover:text-blue-600">Trayectoria</a>
      <a href="#sobre-mi" class="block py-2 text-gray-700 hover:text-blue-600">Sobre mí</a>
      <a href="#contacto" class="block py-2 text-gray-700 hover:text-blue-600">Contacto</a>
    </div>
  </div>
</nav>

<!-- Script para mostrar/ocultar el menú móvil solo en pantallas pequeñas -->
<script is:inline>
  const btn = document.getElementById('menu-btn');
  const menu = document.getElementById('mobile-menu');
  btn.addEventListener('click', () => {
    // Solo alterna el menú si está en móvil
    if (window.innerWidth < 768) {
      menu.classList.toggle('hidden');
    }
  });

  // Oculta el menú móvil si se cambia a pantalla grande
  window.addEventListener('resize', () => {
    if (window.innerWidth >= 768) {
      menu.classList.add('hidden');
    }
  });
</script>
