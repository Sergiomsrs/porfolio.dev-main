<header class="sticky top-0 z-20 mb-4 w-full bg-white/90 dark:bg-neutral-950/90 backdrop-blur-md border-b border-neutral-100 dark:border-neutral-800 shadow-sm">
  <div class="w-full max-w-[740px] mx-auto">
    <nav class="flex justify-between items-center h-16 px-4">
      {/* Logo o nombre */}
            <div class="flex flex-col group"> <!-- Contenedor flex-col para alinear verticalmente -->
        <span class="font-bold text-lg leading-tight tracking-tight text-neutral-900 dark:text-white">
          Sergio Méndez
        </span>
        <span class="text-xs sm:text-sm font-medium text-neutral-500 dark:text-neutral-400 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors duration-200">
          Desarrollo de aplicaciones multiplataforma
        </span>
      </div>
      
      {/* Menú compacto */}
      <div class="flex gap-1">
        {["Proyectos", "Trayectoria", "Sobre mí", "Contacto"].map((item) => (
          <a
            href={`#${item.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/\s+/g, "-")}`}
            class="relative px-3 py-2 text-sm font-medium text-neutral-700 dark:text-neutral-300 hover:text-black dark:hover:text-white transition-all"
          >
            {item}
          </a>
        ))}
      </div>
    </nav>
  </div>
</header>